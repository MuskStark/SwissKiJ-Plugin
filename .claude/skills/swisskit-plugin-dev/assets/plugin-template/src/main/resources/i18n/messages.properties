# {{Name}} plugin i18n — default/English.
# Keys MUST share the plugin.<slug>. prefix and be mirrored in messages_zh.properties.
# Missing keys fall back to the default (English) bundle, so keep both files in sync.

plugin.{{slug}}.name={{Name}}
plugin.{{slug}}.desc=A SwissKitJ plugin. Replace with a one-line description.
plugin.{{slug}}.input=Input
plugin.{{slug}}.input.prompt=Enter input...
plugin.{{slug}}.output=Output
plugin.{{slug}}.output.prompt=Result appears here
plugin.{{slug}}.action.run=Run
plugin.{{slug}}.action.clear=Clear
