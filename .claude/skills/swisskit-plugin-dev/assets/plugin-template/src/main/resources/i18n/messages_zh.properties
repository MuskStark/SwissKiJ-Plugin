# {{Name}} 插件 i18n —— 中文。
# 键必须与 messages.properties 完全一致（同样的 plugin.<slug>. 前缀）。
# 缺失的键会回退到默认（英文）bundle，因此两份文件务必保持同步。

plugin.{{slug}}.name={{Name}}
plugin.{{slug}}.desc=一个 SwissKitJ 插件。请替换为一句描述。
plugin.{{slug}}.input=输入
plugin.{{slug}}.input.prompt=请输入...
plugin.{{slug}}.output=输出
plugin.{{slug}}.output.prompt=结果显示在此
plugin.{{slug}}.action.run=运行
plugin.{{slug}}.action.clear=清空
